# API keys / secrets
.env
bridge/.env

# Factorio server runtime (personal)
.factorio-server/
.factorio-server-data/
saves/
logs/

# factorioctl (cloned separately)
factorioctl/

# Generated MCP configs (per-agent)
bridge/.mcp-config*.json

# Session persistence (runtime)
bridge/.sessions.json
bridge/.session-*.json

# Per-run telemetry event logs (the durable record nauvis-dispatch.py banks from) + agent memory
runs/
.agent_memory.json
bridge/.task-state-*.json

# Binary integrity hash (runtime)
bridge/.factorioctl-hash

# Python
__pycache__/
*.pyc
.venv/
venv/

# Relay
relay/node_modules/
relay/package-lock.json
relay/.dev.vars
relay/.wrangler/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude Code
.claude/
